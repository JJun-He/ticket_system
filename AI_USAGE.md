# AI 활용 기록

이 프로젝트는 과제에서 허용한 AI 도구를 개발 보조 수단으로 사용했습니다. AI가 결과물을 대신 결정하도록 두지 않고, 설계 선택과 최종 검증은 실행 가능한 코드와 테스트를 기준으로 판단했습니다.

## 사용한 도구

- OpenAI Codex
- 공식 기술 문서와 로컬 개발 환경

## 활용 범위

### 요구사항과 설계 검토

- 과제의 필수 기능과 과도한 기능의 경계를 구분했습니다.
- Prisma와 TypeORM, PostgreSQL 행 잠금과 Redis 분산 락, 모놀리스와 마이크로서비스의 장단점을 비교했습니다.
- 좌석 예매의 핵심 위험을 중복 판매, 재시도 중복 처리, 교착 상태와 무제한 락 대기로 정의했습니다.

### 구현 보조

- NestJS 모듈, DTO, TypeORM 엔티티와 migration 작성을 보조받았습니다.
- JWT 인증, Argon2 비밀번호 해시, 예매 트랜잭션과 멱등성 처리를 구현했습니다.
- Dockerfile, Docker Compose, Swagger, 보안 헤더, 요청 제한, ESLint, Prettier와 GitHub Actions CI 구성을 작성했습니다.
- 정상 흐름뿐 아니라 경합과 실패 상황을 다루는 E2E 테스트 케이스를 도출했습니다.

### 디버깅과 개선

- NestJS 11과 호환되지 않는 최신 Swagger 패키지를 강제 설치하지 않고 peer dependency를 확인해 호환 버전을 선택했습니다.
- TypeScript 설정, JWT 패키지 모듈 호환성, TypeORM DataSource와 migration 실행 문제를 실제 빌드 오류를 기준으로 수정했습니다.
- 기존 개발 DB를 보존하면서 자동 스키마 동기화에서 migration 방식으로 전환했습니다.

## 사람이 직접 내린 핵심 판단

- 데이터 정합성의 기준을 애플리케이션 메모리나 Redis가 아니라 PostgreSQL로 정했습니다.
- 좌석 행을 항상 ID 순으로 잠가 교착 가능성을 줄이고, DB 고유 제약을 최종 방어선으로 사용했습니다.
- 현재 범위에는 모듈형 모놀리스가 적합하다고 판단해 Redis, 메시지 브로커, 마이크로서비스와 Kubernetes를 도입하지 않았습니다.
- 결제, 환불, 취소와 좌석 임시 점유는 과제의 핵심 범위를 벗어나므로 구현하지 않았습니다.
- 기능 수보다 실패 처리, 재현 가능한 실행 환경, 테스트와 문서화를 우선했습니다.

## AI 결과 검증 방법

AI가 제안하거나 작성한 코드는 다음 절차를 통과해야만 반영했습니다.

1. TypeScript production build
2. ESLint 및 Prettier 형식 검사
3. 빈 PostgreSQL에서 전체 migration 적용
4. 엔티티와 실제 DB 스키마 차이 확인
5. seed 재실행 안전성과 미래 상영 생성 확인
6. HTTP 수준 E2E 테스트
7. 동일 좌석과 동일 멱등키 동시 요청 테스트
8. DB 고유 제약 충돌의 안정적인 오류 응답 확인
9. 예매 중 강제 DB 오류를 통한 전체 트랜잭션 rollback 테스트
10. 서로 다른 NestJS 인스턴스 간 좌석 경합 테스트
11. production Docker 이미지 빌드와 health check
12. npm 운영 의존성 보안 감사

검증에 실패한 제안은 오류 출력을 근거로 수정하거나 폐기했습니다. 예를 들어 호환되지 않는 패키지를 `--force`로 설치하거나, 테스트 없이 새로운 분산 인프라를 추가하는 방식은 사용하지 않았습니다.

## 한계와 향후 검토

- 부하 테스트 수치는 실행 환경의 영향을 크게 받으므로 과제 결과에 임의의 처리량을 제시하지 않았습니다.
- 운영 트래픽과 병목 지표가 확인되기 전에는 캐시, 메시지 브로커, 검색 엔진을 선제적으로 도입하지 않습니다.
- 실제 운영 전에는 secret manager, TLS 종료 지점, 백업·복구 정책과 외부 모니터링 연동이 추가로 필요합니다.
